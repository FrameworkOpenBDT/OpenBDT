package io.openbdt.setup;

import org.springframework.stereotype.Component;

/**
 * 
 * Symbolic type for adapter setup classes. 
 * A setup class will behave like a Spring component (bean) that is going 
 * to be injected in the project main launcher class at its initialization process. 
 * @author splait
 *
 */

public interface Setup {}
